import { Typography } from "@mui/material";
import { Stack } from "@mui/system";

export const NoConnectionView = () => (
    <Stack>
        <Typography variant="h1" textAlign={'center'} top={'50%'}>No connection :/</Typography>
    </Stack>
);